query RecentUserSessions($userId: UUID!) {
practiceSessionsCollection(filter:{userId:{eq:$userId}}){
    edges{
      node{
        startTime
        endTime
        movement {
            name
        }
      }
    }
  }
}
