query RecentlyDeletedSessions($userId: UUID!) {
  practiceSessionsCollection(
    filter: { userId: { eq: $userId }, deleted: { eq: true } }
  ) {
    edges {
      node {
        ...PracticeSessionDetails
      }
    }
  }
}
