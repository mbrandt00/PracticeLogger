query RecentUserSessions($userId: UUID!) {
  practiceSessionsCollection(
    filter: { userId: { eq: $userId }, deleted: { eq: false } }
    orderBy: { endTime: DescNullsFirst }
  ) {
    edges {
      node {
        ...PracticeSessionDetails
      }
    }
  }
}
